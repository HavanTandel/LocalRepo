name: env variable demo

on: 
  push:
    branches:
      - main
env:
  myman: Github

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name : Print env and configuration variable
      run : |
        echo Name is $myman
        echo Second Name is ${{vars.name}}

name: "demo"
on:
  workflow_dispatch:
    
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Print hello world
        run: echo Hello My Man2
        shell: bash








